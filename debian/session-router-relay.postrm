#!/bin/sh

set -e

if [ "$1" = purge ]; then
    ucf --purge /etc/session-router/relay.ini
    ucfr --purge session-router-relay /etc/session-router/relay.ini
    rm -f /etc/session-router/relay.ini
    rm -rf /var/lib/session-router/relay/nodedb
fi

#DEBHELPER#

